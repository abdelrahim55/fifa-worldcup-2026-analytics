# Streamlit Community Cloud notes

- Entrypoint: `app.py`
- Dependency file: `requirements.txt`
- Python version: select a currently supported Python version in Advanced settings if needed.
- The SQLite warehouse is generated automatically on first use of the SQL Analytics page from the CSV files under `data/raw/`.
- No secrets are required for this project.
